<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ip_a.View.AppPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ip_a.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="using:ip_a.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:AppPageViewModel}"
    mc:Ignorable="d">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="4" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="10">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>

                <TextBlock FontSize="17"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"
                           Text="{x:Bind ViewModel.TextValue, Mode=TwoWay}" />
                <Button Grid.Column="1"
                        Content="Copy IP"
                        IsEnabled="{x:Bind ViewModel.CopyBtnEnabled, Mode=OneWay}"
                        Command="{x:Bind ViewModel.CopyValueCommand}" />
                <Button Grid.Column="2"
                        Content="Refresh"
                        IsEnabled="{x:Bind ViewModel.RefreshBtnEnabled, Mode=OneWay}"
                        Command="{x:Bind ViewModel.ResolvePublicIpCommand}" />
            </Grid>
        </StackPanel>

        <ProgressBar Grid.Row="1" IsIndeterminate="{x:Bind ViewModel.ProgressBarEnabled,Mode=TwoWay}" />
    </Grid>
</Page>