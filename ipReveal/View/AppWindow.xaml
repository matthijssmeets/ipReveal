<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx
    xmlns:winex="using:WinUIEx"
    MinHeight="400"
    MinWidth="600"
    x:Class="ip_a.AppWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ip_a"
    xmlns:local1="using:ip_a.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ipReveal">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <MenuBar>
            <MenuBarItem Title="Tools">
                <MenuFlyoutItem Text="ipReveal" Click="ToAppPage_Click" />
                <MenuFlyoutItem Text="Speedtest" Click="ToSpeedPage_Click" />
                <!--<MenuFlyoutSubItem Text="New">
                    <MenuFlyoutItem Text="Plain Text Document" />
                    <MenuFlyoutItem Text="Rich Text Document" />
                    <MenuFlyoutItem Text="Other Formats..." />
                </MenuFlyoutSubItem>-->
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Exit" Click="AppExit_Click" />
            </MenuBarItem>
            <!--<MenuBarItem Title="Options">
                <MenuFlyoutItem Text="Output" />
                <MenuFlyoutSeparator />
                <RadioMenuFlyoutItem Text="Landscape" GroupName="OrientationGroup" />
                <RadioMenuFlyoutItem Text="Portrait" GroupName="OrientationGroup" IsChecked="True" />
                <MenuFlyoutSeparator />
                <RadioMenuFlyoutItem Text="Small icons" GroupName="SizeGroup" />
                <RadioMenuFlyoutItem Text="Medium icons" IsChecked="True" GroupName="SizeGroup" />
                <RadioMenuFlyoutItem Text="Large icons" GroupName="SizeGroup" />
            </MenuBarItem>-->
        </MenuBar>

        <Frame
            Grid.Row="1"
            x:Name="MainFrame" />
    </Grid>
</winex:WindowEx>